## Changelog

All notable changes to this project will be documented in this file.

This project follows Semantic Versioning for public releases.

### Unreleased

- Add snapshot artifacts (`link snapshot`) and snapshot diffs (`link diff`).
- Add stack-aware route indexing for Express and Laravel (best-effort).
- Add local command history, heuristic architecture rules, and git working-tree structural summaries.

